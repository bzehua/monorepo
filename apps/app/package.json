{
  "name": "@bzehua/app",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "lint": "eslint src --fix",
    "lint:check": "eslint src",
    "format": "prettier --write src",
    "format:check": "prettier --check src",
    "code-quality": "pnpm lint:check && pnpm format:check",
    "code-fix": "pnpm format && pnpm lint",
    "test": "jest"
  },
  "dependencies": {
    "@bzehua/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1"
  }
}
